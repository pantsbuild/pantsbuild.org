---
title: poetry
---

import Option from "@site/src/components/reference/Option";
import styles from "@site/src/components/reference/styles.module.css";

---

Used to generate lockfiles for third-party Python dependencies.

Backend: <span className="color--primary">`pants.core`</span>

Config section: <span className="color--primary">`[poetry]`</span>

<span className={styles.options}>

## Basic options

None

## Advanced options

### `version`

<Option
  cli_repr={`--poetry-version=<str>`}
  env_repr='PANTS_POETRY_VERSION'
  default_repr={`poetry==1.1.8`}
>

Requirement string for the tool.

</Option>

### `extra_requirements`

<Option
  cli_repr={`--poetry-extra-requirements="['<str>', '<str>', ...]"`}
  env_repr='PANTS_POETRY_EXTRA_REQUIREMENTS'
  default_repr={`[]`}
>

Any additional requirement strings to use with the tool. This is useful if the tool allows you to install plugins or if you need to constrain a dependency to a certain version.

</Option>

### `interpreter_constraints`

<Option
  cli_repr={`--poetry-interpreter-constraints="['<str>', '<str>', ...]"`}
  env_repr='PANTS_POETRY_INTERPRETER_CONSTRAINTS'
  default_repr={`["CPython>=3.6"]`}
>

Python interpreter constraints for this tool.

</Option>

## Deprecated options

None

</span>

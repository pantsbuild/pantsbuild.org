---
title: jvm-generate-lockfiles
---

import Option from "@site/src/components/reference/Option";
import styles from "@site/src/components/reference/styles.module.css";

---

```
pants jvm-generate-lockfiles [args]
```

Generate lockfiles for JVM tools third-party dependencies.

Backend: <span className="color--primary">`pants.core`</span>

Config section: <span className="color--primary">`[jvm-generate-lockfiles]`</span>

<span className={styles.options}>

## Basic options

### `resolve`

<Option
  cli_repr={`--jvm-generate-lockfiles-resolve="['<str>', '<str>', ...]"`}
  env_repr='PANTS_JVM_GENERATE_LOCKFILES_RESOLVE'
  default_repr={`[]`}
>

Only generate lockfiles for the specified resolve(s).

Resolves are the logical names for tool lockfiles which are the options scope for that tool such as `junit`.

For example, you can run `./pants jvm-generate-lockfiles --resolve=junit to only generate lockfiles for the `junit` tool.

If you specify an invalid resolve name, like &#x27;fake&#x27;, Pants will output all possible values.

If not specified, Pants will generate lockfiles for all resolves.

</Option>

## Advanced options

None

## Deprecated options

None

## Related subsystems

- [coursier](../subsystems/coursier.mdx)
- [pex](../subsystems/pex.mdx)
- [python](../subsystems/python.mdx)
- [python-bootstrap](../subsystems/python-bootstrap.mdx)

</span>

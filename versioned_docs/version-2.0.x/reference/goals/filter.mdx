---
title: filter
---

import Option from "@site/src/components/reference/Option";
import styles from "@site/src/components/reference/styles.module.css";

---

```
pants filter [args]
```

Filter the input targets based on various criteria.

Backend: <span className="color--primary">`pants.core`</span>

Config section: <span className="color--primary">`[filter]`</span>

<span className={styles.options}>

## Basic options

### `output_file`

<Option
  cli_repr={`--filter-output-file=<path>`}
  env_repr='PANTS_FILTER_OUTPUT_FILE'
  default_repr={`None`}
>

Output the goal&#x27;s stdout to this file. If unspecified, outputs to stdout.

</Option>

### `sep`

<Option
  cli_repr={`--filter-sep=<separator>`}
  env_repr='PANTS_FILTER_SEP'
  default_repr={`\\n`}
>

String to use to separate lines in line-oriented output.

</Option>

### `target_type`

<Option
  cli_repr={`--filter-target-type="[[+-]type1,type2,..., [+-]type1,type2,..., ...]"`}
  env_repr='PANTS_FILTER_TARGET_TYPE'
  default_repr={`[]`}
>

Filter on these target types, e.g. `resources` or `python_library`.

</Option>

### `address_regex`

<Option
  cli_repr={`--filter-address-regex="[[+-]regex1,regex2,..., [+-]regex1,regex2,..., ...]"`}
  env_repr='PANTS_FILTER_ADDRESS_REGEX'
  default_repr={`[]`}
>

Filter on target addresses matching these regexes.

</Option>

### `tag_regex`

<Option
  cli_repr={`--filter-tag-regex="[[+-]regex1,regex2,..., [+-]regex1,regex2,..., ...]"`}
  env_repr='PANTS_FILTER_TAG_REGEX'
  default_repr={`[]`}
>

Filter on targets with tags matching these regexes.

</Option>

## Advanced options

None

## Deprecated options

### `type`

<Option
  cli_repr={`--filter-type="[[+-]type1,type2,..., [+-]type1,type2,..., ...]"`}
  env_repr='PANTS_FILTER_TYPE'
  default_repr={`[]`}
  removal_version='2.1.0.dev0'
  removal_hint={'Use `--target-type` instead of `--type`.'}
>

Filter on these target types, e.g. `resources` or `python_library`.

</Option>

### `regex`

<Option
  cli_repr={`--filter-regex="[[+-]regex1,regex2,..., [+-]regex1,regex2,..., ...]"`}
  env_repr='PANTS_FILTER_REGEX'
  default_repr={`[]`}
  removal_version='2.1.0.dev0'
  removal_hint={'Use `--address-regex` instead of `--regex`.'}
>

Filter on target addresses matching these regexes.

</Option>

### `target`

<Option
  cli_repr={`--filter-target="[[+-]spec1,spec2,..., [+-]spec1,spec2,..., ...]"`}
  env_repr='PANTS_FILTER_TARGET'
  default_repr={`[]`}
  removal_version='2.1.0.dev0'
  removal_hint={'`--filter-target` was removed because it is similar to `--filter-address-regex`. If you still need this feature, please message us on Slack (https://www.pantsbuild.org/docs/community).'}
>

Filter on these target addresses.

</Option>

### `ancestor`

<Option
  cli_repr={`--filter-ancestor="[[+-]spec1,spec2,..., [+-]spec1,spec2,..., ...]"`}
  env_repr='PANTS_FILTER_ANCESTOR'
  default_repr={`[]`}
  removal_version='2.1.0.dev0'
  removal_hint={'`--filter-ancestor` was removed because it is not trivial to implement. If you still need this feature, please message us on Slack (https://www.pantsbuild.org/docs/community).'}
>

Filter on targets that these targets depend on.

</Option>

</span>
